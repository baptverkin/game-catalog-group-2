
{% extends "layout.njk" %}

{% block content %}
  {# <h1>{{game.name}}</h1>
  <img src={{game.cover.url}}>
  <h3>Genre: {{game.genres}}</h3>
  <h3>Summary : {{game.summary}}</h3>
  <h3>Platform : {{game.platform.name}}</h3>


  <form method="POST" action="/add-cookie/{{game.slug}}">

    <button type="submit">
    Add to cart
  </button>

</form> #}

<center><h2><u>{{ game.name }}</u></h2></center>
    <div>
      <center>
        <fieldset>
          {% if game.cover.url %}
            <img src="{{game.cover.url}}">
          {% else %}
            <img src="/images/No_image_available.png" height="160px" width="160px">
          {% endif %}
        </fieldset>
      <center>
      <table>
        <tbody>
          <tr>
            <td>
              <ul>
                <div>
                  <li><u>Genre:</u> </li>
                    <ul>
                      <li>{{ game.genres }}</li>
                    </ul>
                  <li><u>Platform:</u> </li>
                      <ul>
                        <li>{{ game.platform.name }}</li>
                      </ul>
                  <li><u>Summary:</u> </li>
                  <p>
                    {{game.summary}}
                  </p>
                </div>
              </ul>
            </td>
          </tr>
        </tbody>
      </table>
    </div>

    <form method="POST" action="/add-cookie/{{game.slug}}">
      <button type="submit" class="btn btn-secondary">
       Add to cart
      </button>
    </form>

<footer class=" text-center text-white">
  <div style="background-color: papayawhip">
    &nbsp;
  </div>
</footer>


{% endblock %}
